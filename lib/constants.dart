import 'package:flutter/material.dart';

const kCircle = Icon(
  Icons.radio_button_unchecked,
  size: 60,
  color: Colors.white,
);

const kCross = Icon(
  Icons.close,
  size: 75,
  color: Colors.red,
);
